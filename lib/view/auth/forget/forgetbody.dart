import 'package:flutter/material.dart';

import 'forgetview.dart';

class Forgetbody extends StatelessWidget {
  const Forgetbody({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Forgetview(),
    );
  }
}
