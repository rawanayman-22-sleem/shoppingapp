import 'package:flutter/material.dart';

import 'favoriteview.dart';

class Favoritboby extends StatelessWidget {
  const Favoritboby({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Favoriteview(),
    );
  }
}
